modules:
  - type: dnf
    install:
      packages:
        - yt-dlp
        - zsh

  - type: fonts
    fonts:
      nerd-fonts:
        - JetBrainsMono

  - type: script
    scripts:
      - install-rose-pine.sh

  - type: gschema-overrides
    include:
      - patomic.gschema.override